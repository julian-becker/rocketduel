import React from 'react';
import Slider from '../assets/slider.svg';

const SliderKnob = () => {

  return ( 
    <Slider height={16} style={{transform: [ { rotate: "90deg" } ]}}/>
  )
};

export default SliderKnob;